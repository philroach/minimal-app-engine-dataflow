import setuptools

setuptools.setup(
   name='MIN-AE-DATAFLOW',
   version='1.0.0',
   install_requires=[],
   packages=setuptools.find_packages(),
   author='Phil Roach',
   url='https://github.com/philroach/minimal-app-engine-dataflow',
   author_email='philiproach@fastmail.fm'
)

